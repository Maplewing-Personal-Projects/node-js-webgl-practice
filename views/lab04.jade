extend layout
block content
  table(border='1')
    tr
      th i
      th *
    - for( var i = 1 ; i <= n ; i++){
      tr
        td= i
        td(style='text-align: left; padding-left: 20px;')
          - for( var j = 0 ; j < i ; j++ )
            *
    - }